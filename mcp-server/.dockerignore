# Build artefacts
node_modules
dist

# Runtime state (mounted as volume at runtime)
data
# Exception: bake Coach read-only prompt into the image
!data/coach
!data/coach/system_prompt.md

# Version control
.git
.gitignore

# IDE / OS
.vscode
.idea
.DS_Store

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.*

# Docs
# (CLAUDE.md is intentionally kept — Coach loadBaseDddGuide reads it as the
# DDD domain expert guide; the Dockerfile bakes it into /app/CLAUDE.md.)

# Docker
Dockerfile
.dockerignore
